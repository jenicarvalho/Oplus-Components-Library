import styled from 'styled-components';

export const Container = styled.div`
  input {
    border: 1px solid #ddd;
    padding: 8px 10px;

    &:focus {
      outline: none;
      border-color: #aaa
    }
  }
`;
